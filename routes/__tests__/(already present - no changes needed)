const { render, screen } = require('@testing-library/react');
const BusManagement = require('../BusManagement');

test('renders BusManagement component', () => {
	render(<BusManagement />);
	const linkElement = screen.getByText(/Bus Management/i);
	expect(linkElement).toBeInTheDocument();
});

test('checks functionality of BusManagement', () => {
	// Add relevant test cases to verify the component's functionality
});
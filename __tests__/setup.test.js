// Global setup for tests
beforeEach(() => {
  // Global setup code can go here
});

afterEach(() => {
  // Cleanup after each test
  jest.clearAllMocks();
});

afterEach(() => {
  // Cleanup code can go here
});